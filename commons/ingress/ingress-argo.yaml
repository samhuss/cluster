apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argocd-server
  namespace: cert-manager
spec:
  entryPoints:
    - websecure
    # - web
  routes:
  - match: Host(`argocd.dev.raseedy.io`) 
    kind: Rule
    services:
    - name: argocd-server
      kind: Service
      namespace: argocd
      port: 8443
      # scheme: https
    # middlewares:  
    # - name: kk-auth
  # tls:
  #   secretName: raseedy-tls-stg
  #   domains:
  #   - main: "*.raseedy.io"
  #     sans:
  #       - "argocd.dev.raseedy.io"
  # tls:
  #     certResolver: myresolver
  #     domains:
  #     - main: "*.raseedy.io"
---

# apiVersion: traefik.containo.us/v1alpha1
# kind: IngressRoute
# metadata:
#   name: argo-server
#   namespace: ingress
# spec:
#   entryPoints:
#     - websecure
#   routes:
#   - match: Host(`argocd.dev.raseedy.io`) 
#     kind: Rule
#     services:
#     - name: argo-server
#       namespace: argo
#       port: 2746
#       scheme: https
#     middlewares:  
#     - name: kk-auth
#   tls:
#     secretName: raseedy-io-tls
#     domains:
#     - main: "*.raseedy.io"
#   # tls:
#   #     certResolver: myresolver
#   #     domains:
#   #     - main: "*.raseedy.site"

      
